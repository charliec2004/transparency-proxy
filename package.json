{
  "name": "transparency-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Punk-software demo: a passthrough proxy for the OpenAI Responses API that reveals the hidden payload Codex sends in your name.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "mock": "node test/mock-upstream.js",
    "smoke": "bash test/smoke.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
